<div class="col-lg-6">
	<div class="service-item">
		{% set image = paragraph.field_icon.entity.uri.value %}
		<img width="64" height="64" src="{{ file_url(image) }}" alt="{{ paragraph.field_title.value }}"/>

		<h4>{{ paragraph.field_title.value }}</h4>
		<p>{{ paragraph.field_description.value }}</p>
	</div>
</div>
